---
import fetchApi from '../lib/strapi.ts';

const articles = await fetchApi({
  endpoint: 'blog-posts', // the content type to fetch
  wrappedByKey: 'data', // the key to unwrap the response
});

console.log(articles);
---

This is the strapi page